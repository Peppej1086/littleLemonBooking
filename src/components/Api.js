export function fetchAPI(date) {
    const possibleTimes = []; // Effettua la chiamata all'API reale per ottenere gli orari disponibili in base alla data
    console.log(possibleTimes); // Stampa gli orari di prenotazione disponibili per la data specificata
    return possibleTimes; // Restituisce l'array degli orari disponibili
}


export function submitAPI(formData) {
    // Effettua la chiamata all'API reale per inviare i dati del modulo di prenotazione
    const success = true; // Indica se l'invio è riuscito o meno
    console.log(success);
}
